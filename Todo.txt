1. Read and load style defaults from xml or JSON on start
=
=
4. Add a basic login system
5. Create Drawn Components
    a. ItemChooser
    b. Checkbox
    c. MenuBar
    d. TabbedPanel
    e. VideoPlayer
    f. ImageDisplay
    g. WebDisplay
6. Save and load current frame state when
    a. Changing panels
    b. Closing, Minimizing frame
7. Create Default LayoutManager
    a. Allow for custom layout managers
8. Stop Frame and Panel from extending swing classes and just host them
9. Create attributes
    a. inputType (Text, Int, Float, Email, Phone, Password) - For TextInput
    b. wrapText
    c. displayLineNumbers
    d. spellcheckEnabled
    e. active (diabled doesnt draw or allow interactions, this will just gray it out)
    f. resizeMode (Stretch, Stay, Flow)
    g. shadowedText
    h. borderRadius
    i. scrollerlineColor
    j. scrollerBorderColor
    k. scrollbarBorderColor
    l. tooltip
    m. alpha
    n. voiceRecognitionEnabled
    o. backgroundImage
    p. displayAsCarousel
    q. autoPlayVideo
    r. downloadStreamedVideos
    s. tabIndex
    t. selectionMode
    u. shapeBorderStyle
10. Add spellcheck to text type text inputs
=
11. Account for font styles in graphics
=
=
14. Create window managers
15. Ctrl-s & Ctrl-o to frame as global hotkeys to save/load frame states
16. Allow Dragging on scroller
17. Create method to transition between panels when setActive is called
    a. Allow creation of custom transitions
18. Make ContainerComponent abstract class and extend drawnComponent
19. Check for bug in resizingComponents when complist is horizontal
20. Allow VideoPlayer to read streams from internet or play local VideoPlayer
21. Create Shape interface and abstract Polygon class as drawncomponents
    a. include methods for
        - Calculating area
        - Calculating Perimiter
        - Checking contains Point
        - Moving points
22. Add voice recognition
23. Enable tab indicies
24. Create datamanager to handle import
    a. Allow for SQL, JSON, TXT and xml
25. Save and load default style options to config file
26. Add single, interval and multiple interval selection modes to complist
27. Create interpreter for a text based language to engage the ui package quickly
28. Allow shape border attibute to be
    a. snug
    b. bounderies
29. Create method in drawing component to show used attributed and an abstract method to fetch used attributes from extending classes
